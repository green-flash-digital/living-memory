meta {
  name: Check household slug
  type: http
  seq: 3
}

get {
  url: {{API_DOMAIN}}/api/onboarding/validate-slug/:slug
  body: none
  auth: inherit
}

params:path {
  slug: the-turanos
}

settings {
  encodeUrl: true
  timeout: 0
}
