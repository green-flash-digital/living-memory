meta {
  name: Set onboarding step
  type: http
  seq: 4
}

post {
  url: {{API_DOMAIN}}/api/onboarding/set-step
  body: json
  auth: inherit
}

body:json {
  {
    "step": "USER_INFO"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
